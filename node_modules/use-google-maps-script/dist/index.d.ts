import { LoadScriptUrlOptions } from './utils/createUrl';
export { Libraries } from './utils/createUrl';
export interface UseLoadScriptOptions extends LoadScriptUrlOptions {
    id?: string;
    nonce?: string;
}
export declare function useGoogleMapsScript({ id, version, nonce, googleMapsApiKey, googleMapsClientId, language, region, libraries, channel, mapIds, }: UseLoadScriptOptions): {
    isLoaded: boolean;
    loadError: Error | undefined;
    url: string;
};
